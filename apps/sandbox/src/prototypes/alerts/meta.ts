const meta = {
  title: "Alerts",
  description:
    "The promoted Alert primitive in action — semantic status callouts built entirely from design tokens.",
  // A kit demo, not a Madison page: keep it out of sitemap.xml.
  noindex: true,
};

export default meta;
