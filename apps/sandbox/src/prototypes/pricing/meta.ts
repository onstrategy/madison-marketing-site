const meta = {
  title: "Pricing",
  description: "Plan comparison built on-token with Card + Button.",
  // A kit demo, not a Madison page (madisonai.com has no pricing page):
  // keep it out of sitemap.xml.
  noindex: true,
};

export default meta;
