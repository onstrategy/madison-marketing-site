const meta = {
  title: "Book a Demo",
  description: "See a knowledge assistant built from your government's data.",
  // madisonai.com publishes this page at /demo. Matching the live URL exactly
  // preserves it on migration; a redirect would only leak its ranking.
  path: "/demo",
};

export default meta;
