node_modules/
dist/
dist-ssr/
dist-seo-audit/
.turbo/
storybook-static/
*.tsbuildinfo
*.log
.DS_Store
.claude/.skill-gates/
.claude/settings.local.json
# Netlify CLI link state (contains the site id) — the deploy config that IS
# checked in lives in netlify.toml.
.netlify/
.react-router/
